import styled from "styled-components";

export const ButtonsContainer = styled.div`
  display: flex;
  justify-content: flex-end;
  margin: 0 auto;
`;

export const inlineStyles = {
  button: { width: "10rem" },
  deleteButton: { marginRight: "2rem" },
  inputField: { marginBottom: "1rem" },
};
